import PageTitle from "@/components/layout/PageTitle";
import React from "react";

type Props = {};

export default function page({}: Props) {
  return (
    <section>
      <PageTitle title='Withdraw Money' />
    </section>
  );
}
